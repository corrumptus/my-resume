export default function MobileLayout({
    changeLang,
    theme,
    changeTheme
}: {
    changeLang: (lang: "pt-br" | "en") => void,
    theme: string,
    changeTheme: (theme: string) => void
}) {
    return null;
}